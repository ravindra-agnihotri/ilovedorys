services:
  - type: web
    name: dory-bakehouse
    env: node
    buildCommand: |
      apk add --no-cache build-base python3 vips-dev
      npm install --build-from-source sharp
    startCommand: node server.js
